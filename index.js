/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import Character from './src/screens/Character';

AppRegistry.registerComponent(appName, () => Character);
